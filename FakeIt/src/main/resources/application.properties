# Configuraci�n existente
spring.application.name=FakeIt
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com

# --- LOGGING GLOBAL (A�ADE ESTO) --- #
# Logs de peticiones HTTP entrantes (m�todo, URL, headers)
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.web.servlet.DispatcherServlet=DEBUG

# Logs de seguridad (para ver autenticaci�n JWT)
logging.level.org.springframework.security=INFO

# Logs de tu aplicaci�n (reemplaza 'com.tupaquete' por tu paquete base)
logging.level.com.tupaquete=DEBUG

# Formato de logs (opcional)
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
